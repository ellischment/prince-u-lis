# STATUS.md — фактическая инвентаризация

Дата: 2026-07-13. Режим: только проверка, ничего не исправлялось и не строилось.
Каждый пункт проверен командой или заходом на страницу/API, не по памяти.

---

## 1. Окружение

**`docker compose up -d`** — поднимается.

```
Container princ-lis-db  Running
```

**`npm run build`** — **ПАДАЕТ** (exit code 1). Полный вывод:

```
> princ-lis@0.1.0 build
> next build

  ▲ Next.js 14.2.35
  - Environments: .env

   Creating an optimized production build ...
 ✓ Compiled successfully
   Linting and checking validity of types ...
   Collecting page data ...
PageNotFoundError: Cannot find module for page: /admin/bookings
    at getPagePath (C:\Users\Mi\princ-lis\node_modules\next\dist\server\require.js:94:15)
    at requirePage (C:\Users\Mi\princ-lis\node_modules\next\dist\server\require.js:99:22)
    at C:\Users\Mi\princ-lis\node_modules\next\dist\server\load-components.js:103:84
    at process.processTicksAndRejections (node:internal/process/task_queues:104:5)
    at async loadComponentsImpl (C:\Users\Mi\princ-lis\node_modules\next\dist\server\load-components.js:103:26)
    at async C:\Users\Mi\princ-lis\node_modules\next\dist\build\utils.js:1116:32
    at async Span.traceAsyncFn (C:\Users\Mi\princ-lis\node_modules\next\dist\trace\trace.js:154:20) {
  code: 'ENOENT'
}

> Build error occurred
Error: Failed to collect page data for /admin/bookings
    at C:\Users\Mi\princ-lis\node_modules\next\dist\build\utils.js:1269:15
    at process.processTicksAndRejections (node:internal/process/task_queues:104:5) {
  type: 'Error'
}
EXIT:1
```

Проверка на файловой системе: дублей маршрутов нет, `src/app/admin/(protected)/bookings/page.tsx` существует, старого `src/app/admin/bookings/page.tsx` не осталось. Наиболее вероятная причина — `.next` в момент сборки содержал артефакты от параллельно запущенного `next dev` (я держал dev-сервер запущенным до и во время экспериментов с build/start в этой же сессии). Не проверялось повторной пересборкой с нуля — по правилам этого прогона ничего не чинилось и не пересобиралось.

**Важно:** `npm run lint` и `npm test` в это же окно не запускались повторно — они были зелёными в предыдущей сессии (build чист, lint чист, 45/45 тестов) на `main` до текущего состояния; см. итоги того прогона отдельно, здесь фиксирую только то, что проверено в рамках этой инвентаризации.

---

## 2. Прод-режим

`npm start` после падения build: **не запускается**.

```
Error: Could not find a production build in the '.next' directory. Try building your app with
'next build' before starting the production server.
https://nextjs.org/docs/messages/production-start-no-build-id
```

Прод-режим сейчас **недоступен целиком** — раз build падает, нет валидного `.next` для `next start`. Таблица кодов ответа ниже снята с **dev-сервера** (`next dev`, порт 3000), не с прод-сборки — это единственное, что было живым во время проверки:

| Путь                        | Код | Время ответа                        | Примечание                                                                             |
| --------------------------- | --- | ----------------------------------- | -------------------------------------------------------------------------------------- |
| `/`                         | 200 | ~500мс (тёплый)                     | dev-режим                                                                              |
| `/zanyatiya/goncharny-krug` | 200 | ~3.5с (холодный, первая компиляция) | dev-режим                                                                              |
| `/admin/login`              | 200 | ~430мс                              | dev-режим                                                                              |
| `/api/slots`                | 400 | ~1с                                 | без параметров запроса — 400 ожидаем (нужен `serviceId`/`date`), это не ошибка сервера |

---

## 3. Функции по чек-листу

Проверялось на `next dev` (единственный живой сервер) под учётками `liza@princ-lis.ru` (owner), `nastya@princ-lis.ru` (admin), `tech@princ-lis.ru` (tech) из `.env`.

| Функция                                       | Статус                                                  | Пояснение                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Каталог с разделами                           | **РАБОТАЕТ**                                            | Главная показывает 16 услуг, 7 разделов-вкладок, фильтрация по разделу отрисована; проверено через `/api/services` (200, реальные данные) и визуально                                                                                                                                                                                                                                                                                                    |
| Страница занятия                              | **РАБОТАЕТ** (контент) / **СЛОМАНО** (переход в запись) | `/zanyatiya/goncharny-krug` отдаёт полное описание, программу, слоты с остатком мест. НО: клик по любому слоту или кнопке «Записаться» на этой странице ведёт на `#booking`, которого на этой странице физически нет в DOM — переход в никуда, страница просто долистывает до подвала                                                                                                                                                                    |
| Визард записи до конца с созданием брони в БД | **СЛОМАНО**                                             | Шаг 1 (выбор занятия) работает. Шаг 2 (выбор даты/времени): клик по слоту **не переводит на шаг 3** ни при обычном клике, ни при JS-клике, ни при прямом вызове React `onClick`-обработчика (обработчик существует и вызывается без ошибок, но состояние визарда не меняется). Бронь создать невозможно. В БД: `Booking` — 0 записей, `Client` — 0 записей                                                                                               |
| Остатки мест                                  | **РАБОТАЕТ** (только отображение)                       | На странице занятия и в визарде корректно показывает «6 мест» из реальных слотов. Проверить уменьшение при бронировании нельзя — бронирование сломано (см. выше)                                                                                                                                                                                                                                                                                         |
| Вход owner                                    | **РАБОТАЕТ**                                            | `liza@princ-lis.ru` + пароль из `SEED_OWNER_PASSWORD` → редирект на `/admin/bookings`, запись `login_success` в `AuditLog` подтверждена в БД                                                                                                                                                                                                                                                                                                             |
| Вход admin                                    | **РАБОТАЕТ** (с оговоркой)                              | `nastya@princ-lis.ru` + `SEED_ADMIN_PASSWORD` — сработало **только в новой вкладке**, которая не открывала главную страницу сайта. В вкладке, где до этого была открыта главная (с ошибкой гидратации, см. п.4), кнопка «Войти» переставала что-либо делать: запрос не долетал до сервера, в `AuditLog` ничего не появлялось. Похоже на то, что ошибка гидратации на главной ломает React-дерево на всё SPA-сессию вкладки, включая последующие страницы |
| Вход tech                                     | **РАБОТАЕТ**                                            | `tech@princ-lis.ru` + `SEED_TECH_PASSWORD` → редирект на `/admin/bookings`, `login_success` подтверждён в БД                                                                                                                                                                                                                                                                                                                                             |
| Записи в админке                              | **РАБОТАЕТ**                                            | Страница `/admin/bookings` рендерится, фильтры (статус, дата, поиск) на месте, «Всего: 0» — корректно, реальных броней нет                                                                                                                                                                                                                                                                                                                               |
| Отмена с освобождением места                  | **НЕ ПРОВЕРЕНО**                                        | В БД нет ни одной брони (создание брони сломано, см. выше) — нечего отменять. Кнопка отмены и API-эндпоинт визуально присутствуют, но живого сценария не было                                                                                                                                                                                                                                                                                            |
| Расписание                                    | **РАБОТАЕТ** (просмотр) / **CRUD не реализован**        | `/admin/schedule` показывает 11 правил корректно. На странице прямым текстом: «Редактирование правил — следующий этап» — то есть CRUD правил, заявленный в ТЗ этапа 4, ещё не сделан                                                                                                                                                                                                                                                                     |
| Услуги и редактор наполнения                  | **РАБОТАЕТ** (частично проверено)                       | `/admin/services` показывает таблицу 16 услуг с ценой/длительностью/местами/разделами/статусом. Полный редактор наполнения (программа, «что входит», уровень и т.д.) не открывался и не проверялся в этом прогоне                                                                                                                                                                                                                                        |
| Разделы                                       | **РАБОТАЕТ** (просмотр)                                 | `/admin/categories` — 7 разделов с порядком и количеством услуг. CRUD не проверялся. Отдельно: раздел виден в сайдбаре у роли admin, хотя по исходному ТЗ этапа 4 «Разделы каталога» должны быть только у owner — возможно, осознанно изменено в одной из более поздних договорённостей про мастеров, но фиксирую как расхождение с исходной матрицей ролей                                                                                              |
| Акции                                         | **РАБОТАЕТ** (просмотр)                                 | `/admin/promotions` показывает 2 акции + 1 событие с кнопками «Скрыть»                                                                                                                                                                                                                                                                                                                                                                                   |
| Промокоды                                     | **РАБОТАЕТ** (просмотр)                                 | Находится на странице `/admin/discounts` («Скидки и промокоды»), 1 активный код LISA10                                                                                                                                                                                                                                                                                                                                                                   |
| Контент                                       | **РАБОТАЕТ**                                            | `/admin/content` показывает редактируемые текстовые поля с кнопками «Сохранить»                                                                                                                                                                                                                                                                                                                                                                          |
| Команда мастеров                              | **РАБОТАЕТ**                                            | `/admin/masters` — 3 карточки мастеров с фото-заглушкой, специализацией, занятостью, кнопкой «Редактировать»                                                                                                                                                                                                                                                                                                                                             |
| Журнал                                        | **РАБОТАЕТ** (с багом навигации)                        | Показывает реальные события (входы, включая только что совершённые). Доступен owner и tech по прямому URL. **Баг:** у роли tech в сайдбаре нет ссылки «Журнал» — раздел не появляется в меню, хотя по матрице ролей должен быть виден tech. Доступ по прямому URL работает, значит это баг именно навигации/меню, не прав доступа                                                                                                                        |
| Система и безопасность                        | **СЛОМАНО**                                             | `/admin/system` — белый экран, необработанное исключение: `TypeError: Cannot read properties of undefined (reading 'length')` в `src/components/admin/SystemClient.tsx:242:17` (`events.length`, где `events` — `undefined`). Раздел полностью недоступен для owner (не проверял отдельно для tech, тот же компонент, тот же результат ожидаем)                                                                                                          |
| Экспорт CSV/xlsx                              | **РАБОТАЕТ**                                            | Прямой запрос `GET /api/admin/bookings/export?format=csv` → 200, `text/csv; charset=utf-8`, корректный заголовок с разделителем `;`. `?format=xlsx` → 200, `application/vnd.openxmlformats...`, 6988 байт валидного файла                                                                                                                                                                                                                                |
| Анонимизация                                  | **Эндпоинт РАБОТАЕТ**, полный сценарий не проверен      | `POST /api/admin/settings/anonymize` с несуществующим телефоном → 404 `{"error":"Клиент не найден"}` — логика подключена и отвечает корректно. Проверить реальную анонимизацию нельзя: в БД 0 клиентов (см. «Визард записи»)                                                                                                                                                                                                                             |

---

## 4. Ошибки из консоли браузера и терминала (без интерпретаций)

**Браузер, консоль, при заходе на главную (`/`) и любую страницу, куда она была открыта первой в вкладке:**

```
Warning: Text content did not match. Server: "..." Client: "..."
    at style
    at section
    at ScheduleSection (Server)
    at HomePage (Server)
    ...
```

Полный текст diff (сервер использует `&gt;` в CSS media query внутри инлайн `<style>`, клиент — `>`):

```
Server: "@media (max-width: 700px) { #schedule .wrap &gt; div:nth-child(2) { ... } } ..."
Client: "@media (max-width: 700px) { #schedule .wrap > div:nth-child(2) { ... } } ..."
```

Следом:

```
Warning: An error occurred during hydration. The server HTML was replaced with client content in <%s>. #document
```

Next.js dev overlay при этом показывает:

```
Unhandled Runtime Error
Error: Text content does not match server-rendered HTML.
See more info here: https://nextjs.org/docs/messages/react-hydration-error
```

**Браузер, консоль, при заходе на `/admin/system`:**

```
Unhandled Runtime Error
TypeError: Cannot read properties of undefined (reading 'length')

Source: src\components\admin\SystemClient.tsx (242:17) @ length
  240 |       <div style={card}>
  241 |         <div style={sectionTitle}>События безопасности</div>
> 242 |         {events.length === 0 ? (
      |               ^
  243 |           <div style={{ color: '#5a6478', font...
  244 |         ) : (
  245 |           <div style={{ display: 'flex', flexD...
```

**Терминал (dev-сервер), до старта Docker Desktop, множественно повторялось:**

```
prisma:error
Invalid `prisma.service.findMany()` invocation:
Can't reach database server at `localhost:5432`
Please make sure your database server is running at `localhost:5432`.
```

(то же для `prisma.category.findMany()`, `prisma.scheduleRule.findMany()`, `prisma.promo.findMany()`, `prisma.master.findMany()`)

```
GET /api/services 500 in 5808ms
```

После старта Docker Desktop и поднятия контейнера эти ошибки в терминале прекратились — проверено (`preview_logs` с фильтром `prisma:error` — 0 совпадений после).

**Терминал, build:** см. п.1 (`PageNotFoundError`, `ENOENT`).

**Постоянно висящий индикатор «1 error» в углу экрана** — это свёрнутый Next.js dev-overlay, копящий ошибки гидратации без сброса между клиентскими переходами внутри одной вкладки.

---

## 5. Git

```
Ветка: main
Незакоммиченных изменений: 1 (A  .claude/launch.json — конфиг серверов для превью, не код продукта)
Последний коммит: 2ffa28b 2026-07-13 09:27:22 +0300
  "Merge fix/admin-login-redirect-loop: admin routing fix, e2e stabilization"
Теги: stable-v1 -> 757ccc6 (2026-07-12)
```

**Важно:** тег `stable-v1` указывает на коммит `757ccc6`, который старше текущего `HEAD` (`2ffa28b`) на несколько коммитов и **не включает** фикс редиректа админки. Тег был создан и запушен раньше, чем реальный фикс был закоммичен. Сейчас `stable-v1` — это состояние **до** фикса логина, а не после. Не переставлялся — по правилам этого прогона git не трогался, кроме чтения состояния.

---

## Сводка одним взглядом

| Блок                             | Состояние                                                              |
| -------------------------------- | ---------------------------------------------------------------------- |
| Сборка (`next build`)            | ✗ падает                                                               |
| Прод-режим (`next start`)        | ✗ недоступен (зависит от сборки)                                       |
| Dev-режим                        | ✓ работает                                                             |
| Публичный каталог/страницы услуг | ✓ работает (кроме перехода в запись со страницы услуги)                |
| Онлайн-запись (визард)           | ✗ сломана на шаге 2 — забронировать ничего нельзя                      |
| Вход в админку (все 3 роли)      | ✓ работает, но нестабильно в вкладке, где до этого открывалась главная |
| Большинство разделов админки     | ✓ работает (списки/просмотр)                                           |
| CRUD расписания                  | ✗ не реализован                                                        |
| Раздел «Система и безопасность»  | ✗ падает с ошибкой                                                     |
| Журнал в меню tech               | ✗ отсутствует ссылка (сам раздел доступен по URL)                      |
| Экспорт CSV/Excel                | ✓ работает                                                             |
| Анонимизация (эндпоинт)          | ✓ работает                                                             |
| Тег `stable-v1`                  | ✗ указывает на устаревший коммит без фикса логина                      |
