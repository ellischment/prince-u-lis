# Промпт для аудита функционала сайта «Принц и Лис»

Вставить как **первое сообщение** в новом чате с агентом. Не пересказывать заказчице — это скрипт для агента.

---

## Стартовый промпт

Проведи полный аудит сайта «Принц и Лис». Проект — Next.js App Router + TS strict + Prisma + SQLite, панель, русский UI. Тестовый сайт под basic_auth: https://80-78-253-177.sslip.io, логин `studia`, пароль `PrincLisTest24`. Панель `/admin`: почта `owner@princ-lis.ru`, пароль = `SEED_OWNER_PASSWORD` из `/root/app/.env` на сервере. Корень проекта: `C:\Users\Mi\Downloads\FINAL files Princ i lis\princ-i-lis`. Доступ к серверу: `ssh -i C:\Users\Mi\.ssh\princ_deploy_ed25519 root@80.78.253.177`, проект в `/root/app`. Правила проекта — в `CLAUDE.md`, спецификация — `SPEC.md`, состояние — `STATE.md`, план — `PLAN.md`.

Работай строго по этому чек-листу, ничего не пропуская. Для каждого пункта скажи одной строкой: «OK», «Проблема: …», или «Не проверено, потому что …». В конце — сводный отчёт: сколько ОК, сколько проблем, приоритетный список починки.

### 1. Публичный сайт: доступность и оболочка

- [ ] Главная `/` открывается, все секции видны, ошибок в консоли нет.
- [ ] Меню (десктоп и мобильное) — все пункты кликабельны, ведут на нужные страницы.
- [ ] Подвал: контакты, часы работы, ссылки на политику/договор.
- [ ] Единственный H1 на каждой странице (см. `SPEC.md`).
- [ ] На ширине 320px нет горизонтальной прокрутки ни на одной странице.
- [ ] Гирлянда на месте, НЕ привязана к курсору.
- [ ] `favicon.ico` (звезда) и `apple-icon.png` отдаются.
- [ ] `robots.txt`: `Disallow: /`, `X-Robots-Tag: noindex` (тестовый, не боевой).
- [ ] `app/loading.tsx` работает: между переходами видна тонкая полоска сверху.

### 2. Публичный сайт: страницы

Для каждой открыть, посмотреть текст, фото, кнопки; отдельно проверить формы (пункт 3).

- [ ] `/o-nas` (О студии)
- [ ] `/raspisanie` — фильтры, слоты, ссылки на занятия
- [ ] `/zanyatiya/[slug]` — открыть 3 разных занятия; проверить фото, блок «кто ведёт», кнопку записи, отзывы, галерею
- [ ] `/kupit` — все вкладки, карточки, бейдж категории, фильтры (если есть)
- [ ] `/kupit/[slug]` — открыть 2 разных: одну «purchase» (готовая работа/сертификат), одну «booking» (курс/абонемент, если добавлены). Проверить заголовок формы: «Оставить заявку на покупку» vs «Оставить заявку на запись».
- [ ] `/komanda` — сетка мастеров; проверить, что у мастеров есть фото (иначе — заглушка с первой буквой). Составить список мастеров БЕЗ фото.
- [ ] `/komanda/[slug]` — открыть 2 разных мастера
- [ ] `/otprazdnovat` — праздники; список тех, что БЕЗ фото
- [ ] `/sotrudnichestvo` — тексты, форма
- [ ] `/bonusy` — тексты
- [ ] `/blog` — список статей, пагинация
- [ ] `/blog/[slug]` — открыть 2 статьи, проверить Markdown-рендер, оглавление, обложку
- [ ] `/voprosy` — FAQ, разметка `FAQPage`
- [ ] `/kontakty` — карта/адрес/часы/телефон, ссылки Telegram и WhatsApp
- [ ] `/politika` — Политика обработки ПДн
- [ ] `/zapis` — общая форма записи
- [ ] `/404` — несуществующий адрес показывает нашу страницу

### 3. Заявки со стороны сайта (боевой прогон каждого типа)

Отправить по одной заявке каждого типа через живую форму (тестовые данные: имя «Аудит», телефон `+79000000001`, комментарий с меткой даты). Для каждой проверить в БД + amoCRM + Telegram:

- [ ] `type=booking` — со страницы `/zanyatiya/[slug]` через `BookingForm`. Ожидается: сделка в воронке `AMO_PIPELINE_ID` (тест — «ТЕСТ — заявки с сайта», `11261394`), уведомление в Telegram-чат.
- [ ] `type=free_time` — индивидуальное время на `/raspisanie` или отдельная форма.
- [ ] `type=celebration` — с `/otprazdnovat` или карточки праздника.
- [ ] `type=purchase` — с `/kupit/[slug]` для готовой работы/сертификата. Ожидается: воронка `AMO_PIPELINE_ID_PURCHASE` (тест — «Покупки-ТЕСТ», `11261362`).
- [ ] `type=booking через категорию курса` — с `/kupit/[slug]` для товара из категории с `requestKind=booking`. Ожидается: воронка `AMO_PIPELINE_ID` (не `_PURCHASE`).
- [ ] `type=partnership` — с `/sotrudnichestvo`.

Для каждой в БД проверить: `amoStatus=sent`, `amoDealId` заполнен, `lastError=null`, `attempts=1`. В amoCRM: сделка есть, в нужной воронке, тег корректный (`Покупка`/`Запись`/`Праздник`/`Сотрудничество`). В Telegram: сообщение доставлено, ПДн в тексте нет (только тип, занятие/повод, время, ссылка на сделку — правило 152-ФЗ).

Плюс:
- [ ] Дубликат: отправить ту же заявку повторно — должно вернуться `{ok:true, duplicate:true}`, сделка одна.
- [ ] Rate limit: 6-я заявка с того же IP за 10 минут — `429`.
- [ ] Валидация: пустое имя, короткий телефон, пропущенное согласие — понятные ошибки под полями.

### 4. Панель `/admin` — три роли

Проверить под каждой ролью (owner, admin, tech). Пароли — `SEED_*_PASSWORD` из `.env` (у заказчицы). Роль `admin` НЕ видит: разделы, сотрудники, журнал, система, CRM. Роль `tech` видит всё.

- [ ] Вход, выход, «неверный пароль», rate-limit попыток.
- [ ] Роль `admin` действительно не видит запрещённые разделы (проверить прямым URL — должен быть 403).
- [ ] «Сегодня» (`/admin`): сводка дня, «что стоит проверить», поиск.
- [ ] `/admin/requests` — журнал заявок: список, фильтры, поиск, статусы amoStatus, ссылка на сделку, экспорт CSV.
- [ ] `/admin/lessons` — занятия: создать, редактировать, скрыть, удалить пустое; изменить мастера, слоты, формат; загрузить/удалить фото. Убедиться, что после сохранения сайт `/zanyatiya/[slug]` показал новое.
- [ ] `/admin/masters` — мастера: создать, редактировать, часы работы (`MasterAvailabilityRule`), исключения (`MasterException` closed/extra), загрузить фото. Проверить, что фото мастера появилось на `/komanda` и на страницах занятий, где он ведёт.
- [ ] `/admin/shop` — категории 1/2 уровня: создать, переименовать, изменить тип показа (витрина/карточки), изменить **тип заявки** (покупка/заявка на запись — селектор справа), скрыть/удалить. Проверить, что смена типа заявки отразилась в форме на `/kupit/[slug]` (заголовок/кнопка).
- [ ] `/admin/shop` — работы и товары: создать, редактировать, загрузить фото, скрыть.
- [ ] `/admin/celebrations` — праздники: редактировать, фото. Список праздников БЕЗ фото — записать.
- [ ] `/admin/events` — события: создать, редактировать, скрыть.
- [ ] `/admin/reviews` — отзывы: создать, привязать к занятию/мастеру, скрыть.
- [ ] `/admin/blog` — статьи: создать, редактировать Markdown, обложка, публикация/черновик, 301 при смене адреса.
- [ ] `/admin/questions` — вопросы/ответы: создать, редактировать, скрыть.
- [ ] `/admin/team` (тех/владелец): сотрудники студии.
- [ ] `/admin/journal` (тех/владелец): журнал действий панели — фильтры, поиск.
- [ ] `/admin/system` (тех/владелец): служебное — миграции, кэш, бэкапы.
- [ ] `/admin/settings` (владелец): пользователи и роли — создать, изменить роль, сбросить пароль, удалить.
- [ ] Кнопка тестового уведомления в TG — работает.

### 5. Медиа

- [ ] `next/image` работает: фотки не грузятся полноразмерно на превью.
- [ ] Загрузка фото в каждом разделе: изображение сохраняется в `/uploads/`, запись `Media` создаётся с корректным `path`, `width`, `height`.
- [ ] Удаление фото убирает запись из БД и файл с диска (или помечает к удалению).
- [ ] `media-prune` не удаляет свежие файлы (возрастной порог, тир-1 фикс).
- [ ] Полный список владельцев без фото — по каждой сущности (`Master`, `Celebration`, `Event`, `Lesson`, `Work`, `ShopItem`).

### 6. SEO и разметка

- [ ] Каждая публичная страница отдаёт валидный JSON-LD: `Organization`, `WebSite`, страничные — по `SEO.md`.
- [ ] Sitemap `/sitemap.xml` содержит все публичные URL, никаких редиректов.
- [ ] `robots.txt` на тесте закрыт (`Disallow: /`), на бое (когда переедем) — открыт.
- [ ] Canonical, OG, метатеги — есть на каждой странице.

### 7. Безопасность (частично уже пройдено на Стадии B, обновить)

- [ ] HTTPS Let's Encrypt — валиден.
- [ ] `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Content-Security-Policy` — заголовки на месте.
- [ ] Панель НЕ доступна из внешнего интернета без входа.
- [ ] amoCRM/Telegram токены НЕ утекают ни в HTML, ни в логи, ни в ответах API.
- [ ] Middleware проверяет роль на всех `/admin/*`.

### 8. Инфраструктура

- [ ] `docker compose ps`: контейнеры `app`, `caddy` живы.
- [ ] Автобэкап БД: `crontab -l` под root, `04:00`, файлы в `/root/app/backups/`.
- [ ] Пре-деплой копия БД: `scripts/deploy.sh` снимает `predeploy_*.db`.
- [ ] Логи `app` за последние сутки: нет `error`/`ECONN`, кроме известных.
- [ ] Диск: `df -h /` — свободно > 20%.

### Форма отчёта

В конце сведи одним списком:
1. **Всё работает:** N пунктов.
2. **Проблемы (по приоритету):**
   - **P1 (блокирует показ):** …
   - **P2 (важно, но не блокирует):** …
   - **P3 (косметика/наполнение):** …
3. **Наполнение, которое ждёт заказчицу (не баги):** мастера без фото — список; праздники без фото — список; пустые разделы — список.
4. **Что стоит проверить руками в браузере** (то, что автоматом не проверить, но нужно): анимации, гирлянда, поведение мобильного меню, галерея.

Не выкатывай никаких правок в код без явного разрешения. Ошибки только описываешь и предлагаешь план починки.
