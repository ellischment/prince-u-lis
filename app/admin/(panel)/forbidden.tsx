import styles from "./section.module.css";

/**
 * Ответ 403 для разделов панели, закрытых по роли.
 *
 * Раньше такой раздел отдавал 200 со страницей «Недостаточно прав»: человек
 * видел правильное, а машина неправильное. Теперь страница зовёт forbidden(),
 * Next отдаёт 403 и рисует это. Вызов forbidden() включается флагом
 * experimental.authInterrupts в next.config.ts, а сам код ответа держится
 * только пока над панелью нет границы Suspense: заглушка загрузки из
 * app/admin/loading.tsx поэтому убрана (components/LoadingBar.tsx).
 */
export default function Forbidden() {
  return (
    <>
      <h1>Недостаточно прав</h1>
      <p className={styles.denied}>
        Этот раздел доступен владельцу. Если доступ нужен по работе, попросите владельца изменить
        вашу роль в разделе «Настройки и доступы».
      </p>
    </>
  );
}
