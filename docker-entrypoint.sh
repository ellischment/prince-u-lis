#!/bin/sh
set -e

# Миграции применяются при СТАРТЕ контейнера, а не при сборке образа.
# При сборке база ещё не примонтирована, миграция ушла бы в никуда.
echo "Применяю миграции"
npx prisma migrate deploy

echo "Запускаю приложение"
exec node server.js
