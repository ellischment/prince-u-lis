# База данных (Docker: docker compose up -d)
DATABASE_URL="postgresql://princlis:princlis_dev@localhost:5432/princlis"

# NextAuth (генерируй: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Пароли пользователей (применяются при: npx tsx prisma/seed.ts)
# После смены — ОБЯЗАТЕЛЬНО перезапустить seed!
SEED_OWNER_PASSWORD="change-me-in-production"
SEED_ADMIN_PASSWORD="dev-admin-123"
SEED_TECH_PASSWORD="dev-tech-123"
