# REPORT. Сквозная приёмка (SPEC §20, шаг 11.4)

Дата: 2026-08-28. Проверка на локальной боевой сборке (`node .next/standalone/server.js`) и e2e под `SEED_OWNER_*`. Пункты, требующие публичного адреса, отмечены «на стадии B».

## Пройдено (код готов, проверено)
- Телефон 320px без горизонтальной прокрутки — проверено (9.1d, все ключевые страницы).
- Адреса: все 14 верхнеуровневых + типы детальных отвечают 200, каждая по своему адресу.
- Заголовки: ровно один H1 на каждой странице (проверено вживую).
- Доступность: клавиатура, видимый фокус, контраст AA — проверено (9.1d).
- Запись: заявка пишется в базу до внешних систем, уходит в amoCRM (сделка) и Telegram, копия в журнале — проверено вживую (записи и покупки доходят; TG работает).
- Расписание: открыт текущий день, названия ведут на страницы, заявка на свободное время уходит — e2e + код.
- Панель: все разделы открываются и меняют сайт — e2e (SEED_OWNER), чек-лист ARCHITECTURE §15.
- Согласия: без галочки заявка не уходит (серверная валидация), видеоотзыв без согласия не публикуется — e2e.
- Заголовки безопасности: CSP/HSTS/X-Content-Type/Referrer/X-Frame отдаются на всех маршрутах (5/5).
- Загрузка файлов: проверка по содержимому (sharp читает формат из байтов, не по расширению), лимит 10 МБ, пересжатие в webp — `lib/media.ts`.
- Проверка: `npm run check` зелёный — 191 юнит, сборка, 19 e2e.

## На стадии B (нужен тестовый домен/сервер)
- Скорость LCP/INP/CLS на среднем телефоне (SPEC §17) — замер на живом домене.
- Внешние проверки: securityheaders.com ≥ A, ssllabs ≥ A, OWASP ZAP baseline, редирект HTTP→HTTPS.
- Валидаторы разметки Яндекса и Google, разворот ссылки в Telegram-карточку (SEO §14).
- Восстановление из резервной копии на практике (SPEC §20 «Копии»).
- Боевая заявка каждого типа (включая покупку) в тестовую воронку, затем удалить.

## Вывод
Этап 11 закрыт по коду (11.1 безопасность, 11.2 фоновые задачи). Остаются 11.3 (кибербез на тестовом домене) и живая часть 11.4 + этап 12 (перенос, передача) — выполняются на сервере по `DEPLOY.md` стадии A→B. Готовность к стадии B: полная, ждём VPS и домен.
